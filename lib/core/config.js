const config = {
  verbose: true,
  logs: {
    deprecation: true,
    nonExistingField: true,
    classDuplicate: true,
    typeDuplicate: true
  },
  resolving: true,
  defaults: true
};

export default config;
